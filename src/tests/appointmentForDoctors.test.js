import React from 'react';
import { render, screen } from '@testing-library/react';
import AppointmentForDoctors from '../components/AppointmentForDoctors';

/* const mockedData = {
    id: 3,
    doctorId: 4,
    patientId: 1,
    patientName: "James Smith",
    pickedDate: "15/03/2022"
} */

describe('AppointmentForDoctors', () => {
    test('renders ok', () => {
        /* render(
            <AppointmentForDoctors data={mockedData} />
        )
        expect(screen.getByText('James Smith')).toBeInTheDocument()
        expect(screen.getByText('15/03/2022')).toBeInTheDocument()
        expect(screen.getAllByRole('button')).toHaveLength(1)
        expect(screen.getByRole('button')).toHaveAccessibleName('Done') */
    })
})